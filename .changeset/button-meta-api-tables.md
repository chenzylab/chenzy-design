---
"@chenzy-design/svelte": patch
---

补全 Button 组件元数据（components.json）：Button props 补回缺失的 `htmlType`、`icon`，并新增 `subComponents` 描述 ButtonGroup 与 SplitButtonGroup 的 props，使 AI/docs 消费方能拿到三个组件的完整 API。
