/**
 * Lottie constants — ported from Semi semi-foundation/lottie/constants.ts.
 * See specs/components/other/Lottie.spec.md §3.
 * Semi 原名 cssClasses.PREFIX（通用词，本库 core 主入口是扁平单一命名空间，
 * 加 LOTTIE_ 前缀避免与其他组件同名常量冲突，同 anchor/scroll-list）。
 */

export const LOTTIE_PREFIX = 'cd-lottie';
