---
title: Badge 徽标
name: badge
category: show
brief: Badge（徽标）用于在宿主元素（图标、头像、按钮等）的角标位置展示状态提示。
---

## 使用场景

徽标（Badge）用于在宿主元素（图标、头像、按钮等）的角标位置展示状态提示，常见形式包括未读消息数字、小红点以及状态指示点（在线/离线/忙碌）。它依附于某个宿主元素，不独立占据布局空间。

典型使用场景：导航图标上的未读消息计数、头像旁的在线状态、操作按钮上的待处理数量提示。当计数超过设定最大值时自动显示为 N+ 格式。

## 何时使用

当需要在不干扰主体元素的前提下，对其附加一个简短的状态或数量提示时使用 Badge。不适合在没有宿主元素的情况下单独放置，也不应承载较长文案（文案展示请使用 Tag 或 Banner）。

## 无障碍

- Badge 本身为非交互纯展示元素，不需要 `tabindex`，不进入 Tab 序列。
- 宿主元素通过 `aria-describedby` 关联 Badge 内容，使屏幕阅读器可感知数字或状态。
- 纯红点类型的 Badge 加 `aria-hidden="true"`，语义由宿主元素的文案承载；状态点类型使用 `role="status"` 提供语义。
- 不以颜色为唯一信息通道，需同时通过文案或图标区分语义。
