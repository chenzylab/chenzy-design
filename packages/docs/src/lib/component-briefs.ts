// 组件 brief 的英文翻译（中文版来自组件库 meta.description）。
// docs 侧维护，不改组件库。key 为组件名（与 components.json 的 name 一致）。
export const componentBriefsEn: Record<string, string> = {
  Button: 'A button that triggers an immediate action — the most basic interactive primitive.',
  IconButton: 'An icon-only action button — a thin wrapper over Button that forwards all Button props plus icon, and makes ariaLabel required to guarantee an accessible name.',
  Icon: 'A vector icon container with unified size, color, and alignment — a purely presentational primitive.',
  Divider: 'A divider line that visually separates content sections — purely presentational.',
  Space: 'A flex container that applies uniform spacing between child elements.',
  Typography: 'A typography family — Title / Text / Paragraph / Link — that unifies text styles and semantic colors.',
  Grid: 'A 24-column grid layout: a Row container plus Col columns built on flex, with gutter passed down to Col via context; supports xs/sm/md/lg/xl/xxl responsive breakpoints (mobile-first fallback resolution).',
  Layout: 'A page-level layout scaffold: a Layout root container with Header / Sider / Content / Footer subcomponents.',
  Input: 'A single-line text input that supports prefix/suffix, clear, count, password visibility toggle, and IME.',
  TextArea: 'A multi-line text input that supports counting (maxCount / grapheme), clear, validation states, IME, and autosize height (minRows/maxRows).',
  Switch: 'A toggle that switches between two mutually exclusive states instantly, with loading state and custom labels.',
  Checkbox: 'A checkbox usable standalone or within a CheckboxGroup to manage a set of options, with indeterminate state.',
  Radio: 'A radio button, typically placed within a RadioGroup, following the WAI-ARIA radiogroup pattern (roving tabindex + arrow keys).',
  InputNumber: 'A constrained numeric input with stepper buttons, keyboard stepping, precision, and range clamping, reusing the Input visuals.',
  Rating: 'A rating control supporting half stars, hover preview, click-to-clear, and keyboard operation, following the APG Slider pattern.',
  Slider: 'A slider for selecting a value over a continuous or discrete range, supporting single and range values, ticks, vertical orientation, and full keyboard operation.',
  Form: 'A form container that bridges the headless createForm, providing field registration, validation, layout, and accessibility.',
  Select: 'A dropdown selector supporting single / multiple selection, local filtering, keyboard navigation, and an overlay.',
  AutoComplete: 'An autocomplete input supporting local filtering, remote async search, option grouping, inline tags, and keyboard selection.',
  TagInput: 'A tag input supporting Enter / separator submission, backspace deletion, deduplication, a maximum limit, and submit-on-blur.',
  ColorPicker: 'A color picker: a trigger opens an overlay panel (or renders inline) with a saturation square, hue bar, alpha bar, format-switchable input (hex/rgb/hsv/hsl), and presets, communicating externally as a hex string.',
  DatePicker: 'A date picker: type=date calendar panel / type=dateTime date + time (reusing the TimePicker hour-minute-second columns) / type=month month panel / type=year year panel, controlled or uncontrolled; display and weekday/month/year are localized via Intl.DateTimeFormat.',
  RangePicker: 'A date range picker with dual panels (two months side by side, right = left + 1) for sequential selection (click start → click end, auto-sorted, endpoints can span panels, hover previews the interval across panels), controlled or uncontrolled; display and weekday/month are localized via Intl.DateTimeFormat.',
  TimePicker: 'A time picker: single type=time with HH:mm:ss three-column scroll selection, controlled or uncontrolled, display localized via Intl.DateTimeFormat; supports type=timeRange range selection, a format string to control columns, and string value input.',
  Cascader: 'A cascading selector that expands columns level by level on click, selecting a single leaf node.',
  TreeSelect: 'A tree selector with a single expandable/collapsible tree panel, selecting a single leaf or any node.',
  Transfer: 'A transfer that moves items between two list columns, supporting multi-select, move buttons, local search filtering, grouping (groupList), a tree source panel (treeList), one-way migration (oneWay), drag-to-reorder in the target column (draggable), virtualization (virtualize), and remote search (onSearch).',
  Upload: 'A file uploader supporting click and drag-and-drop selection and a file list (name/size/status/remove). Real network upload is out of scope for this iteration.',
  Breadcrumb: 'A breadcrumb navigation showing the current page’s position within the hierarchy, supporting both data-driven routes and declarative Item usage.',
  Pagination: 'A pagination control for navigating page numbers across large datasets, supporting default and simple modes, with page numbers localized via Intl.NumberFormat. Out-of-range current page / page size and quick-jump input are clamped to the valid range (pure-function implementation; controlled props are reported only via onChange).',
  Steps: 'A steps indicator that guides users through a process, supporting horizontal/vertical orientation, fill/nav/basic types, custom icons, clickability, and status display.',
  Tabs: 'A tabs component that organizes and switches between multiple peer content panes in one area, supporting line/card/button types, data-driven tabList or purely declarative <Tabs.Pane> auto-collection, renderTabBar custom tab bar, roving-tabindex keyboard navigation, closable tabs, and overflow scroll/dropdown handling.',
  Dropdown: 'A dropdown menu that surfaces a set of actionable command items from a trigger element, supporting hover/click/contextMenu triggers, nested submenus, divider separators, group grouping, useDismiss, keyboard navigation, and danger items.',
  Menu: 'A data-driven navigation menu supporting vertical/inline/horizontal modes, SubMenu expand/collapse, single/multiple selection highlighting, controlled or uncontrolled; items support type=divider separators and type=group section titles. The purpose prop distinguishes command menus (menu/menuitem semantics) from site navigation (nav landmark + native <a> link semantics).',
  Anchor: 'An anchor navigation as a vertical/horizontal link list, supporting scroll-spy active highlighting, smooth scroll on click, an ink border indicator, a custom scroll container, and multi-level nested link trees, controlled or uncontrolled.',
  Tag: 'A tag with light/solid/ghost states, five semantic colors, closable and checkable; controlled checked/visible are not written back, notifying only via onChange/onClose.',
  Avatar: 'An avatar supporting images (src/srcset), fallback to children/alt initial on load failure, circle/square shapes, enumerated or custom numeric sizes, color=auto hashing color from text, and a status dot in the bottom-right corner. Avatar.Group overlaps multiple avatars, collapsing items+maxCount into a “+M” overflow avatar, with group-level shape/size passed down (child Avatar’s own props take precedence).',
  Badge: 'A badge: wrapper mode shows a count/dot badge in a child’s corner (supporting overflowCount, showZero, position, offset); standalone status mode shows a status dot plus optional text.',
  Card: 'A card container: cover (bleed) → header (title + extra) → body (children, skeleton placeholder while loading) → actions (equal-width footer actions); bordered/shadow/hoverable control the visuals, and aria-labelledby is associated when title is a string.',
  Tooltip: 'A text tooltip that shows a brief description when the trigger element is hovered/focused/clicked, supporting common placements, an arrow, delay, dark/light themes, semantic status icons, and fully controlled custom triggering, with imperative overlay positioning.',
  Popover: 'A popover that displays rich overlay content when the trigger element is clicked/hovered/focused, supporting a title, common positions, alignment, an arrow, and spacing, with pure-CSS overlay positioning.',
  Empty: 'An empty state for no-data / no-search-results / load-failure scenarios, with built-in preset illustrations, a title, description, and an action area.',
  Descriptions: 'A description list that displays read-only fields in groups, supporting data-driven and declarative <Descriptions.Item> usage, inline/row layout, a column count (including responsive breakpoint objects), bordered borders, a colon, horizontal/vertical direction, and align/justify alignment.',
  Collapse: 'A collapse panel that groups content and expands on demand, supporting data-driven panels and declarative <Collapse.Panel> usage (either/or), controlled activeKey, accordion mode, expand animation, arrow position, borders, and panel-level disabled.',
  Timeline: 'A timeline that displays a series of events vertically/horizontally in chronological order. Two usages, either/or: pass dataSource for data-driven, or omit dataSource and write <Timeline.Item> declaratively within children (any rich content). Supports left/right/alternate/center modes, vertical/horizontal direction, a pending ghost node, reverse order, and solid/dashed line styles; alternate layout is determined by pure CSS :nth-child, and both usages share the same structure and styles.',
  List: 'A list with data-driven rendering (dataSource + renderItem), supporting header/footer (string or Snippet), a bordered outer frame, split row dividers, loading skeleton/spinner, and an empty state (Empty by default). A generic component.',
  Image: 'An image supporting lazy loading (native loading=lazy or IntersectionObserver), object-fit/position, placeholders (skeleton or LQIP blur placeholder + main-image fade-in), load-failure fallback (fallback image / default broken image / custom slot), and click-to-fullscreen preview (portal mask + zoom/rotate + Esc to close). Wrapped in Image.PreviewGroup, multiple images can be navigated left/right in preview mode (← → buttons + keyboard arrows).',
  Highlight: 'Purely presentational text highlighting: marks searchWords within a sourceString, wrapping matches in <mark> by default. Outputs plain text (HTML is escaped, not parsed), supporting case sensitivity, first-only/all matches, a regex-escape toggle, and a custom wrapping element/style.',
  VirtualList: 'A virtual-scrolling list foundation (generic): renders only the visible region plus an overscan buffer to render very long lists with high performance. Fixed-height uses O(1) range computation; dynamic height (itemSize=’auto’) estimates via estimatedItemSize + measures with ResizeObserver to backfill the height table, using prefix sums to correct offsets/ranges and compensate scroll offset. Supports vertical/horizontal (horizontal is fixed-width only; dynamic degrades to fixed width by estimatedItemSize). scrollTarget=’self’ (default) scrolls the inner container; ’window’ uses window (document) scrolling with the container stretched to height, suited to full-page long lists (vertical; horizontal degrades to self, both fixed/dynamic reusing window geometry windowScrollTop). Provides an imperative scrollToIndex API. It has no visuals of its own; rows are rendered by renderItem.',
  Carousel: 'A carousel: displays one or multiple slides on screen (slidesToShow/slidesToScroll), supporting slide/fade animation, autoplay (with hoverToPause), loop, dot/line/columnar indicators, prev/next arrows, vertical orientation, pointer drag gestures, and controlled value. Slides are passed in via Snippet[].',
  Tree: 'A tree control: displays nodes hierarchically, supporting expand/collapse, single/multiple selection, checkable parent-child linkage (including mixed half-checked state), and built-in search highlighting. Controlled value/checkedKeys/expandedKeys are not written back, notifying only via onChange/onCheck/onExpandedChange. Keyboard navigation follows WAI-ARIA APG Tree View (single tab stop + aria-activedescendant).',
  Table: 'A table: column-definition-driven rendering, three-state sorting (asc/desc/none), client-side pagination, and row selection (with indeterminate); sortState / rowSelection.selectedRowKeys / pagination.current are controlled and not written back, notifying only via onSortChange / onChange. Reuses @chenzy-design/core pure-function algorithms and the Pagination component. Supports expandable rows (controlled/uncontrolled), fixed columns, column filters (filters/onFilter), resizable column widths (local overrides not written back to columns), and tree data (rows with children auto-nest, the first column gets an expand triangle + indentation, sorting/pagination apply to top-level rows, flattened via the core flattenTreeRows pure function, controlled expanded keys not written back). Tree row selection supports rowSelection.checkStrictly parent-child linkage (default false checks parent with descendants + half-check, true keeps parent/child independent; linkage uses core conductRows/toggleRowCheck pure functions). Row virtual scrolling (virtualized) renders only the in-viewport row slice (reusing core fixedRange), with leading/trailing padding spacer rows holding total height while preserving native table semantics; thead is sticky; mutually exclusive with pagination (pagination is ignored when virtualized).',
  Calendar: 'A month/week/day calendar: a day-cell grid with event display and +N collapse, today/selected/outside/weekend/disabled states, and navigation (prev/today/next, by month/week/day); the mode switches month (6×7) / week (1×7) / day (single-day timeline) views, with day mode listing hour slots vertically (dayStartHour~dayEndHour), timed events placed into their starting-hour slot, and all-day or multi-day spanning events placed in the top “all-day” area; selectionMode switches single (selectedDate) / range (rangeValue, with endpoints + interval highlight + hover preview). Controlled value (anchor) / selectedDate / rangeValue are not written back, notifying only via onChange/onSelect/onRangeChange/onDateClick/onMoreClick. Full WAI-ARIA grid roving keyboard focus (arrows/PageUp-Down/Home-End/Enter-Space), a popup overlay mode (reusing the _floating portal + outside-click/Esc close), and day-view timeline virtualization (reusing core fixedRange).',
  ScrollList: 'A wheel picker: JS-driven snap-to-center positioning with a central selection mask and top/bottom fade, supporting click/keyboard/drag selection, skipping disabled items; controlled value is not written back, notifying only via onChange. Single column (data) is backward-compatible; multiple columns (columns) cascade, with the value being an array of per-column combinations. Advanced capabilities: cyclic looping scroll, inertial physics (pointer drag + exponential deceleration + predictive snap), virtualization (renders only the viewport when items are numerous), loadMore (load more on reaching the end), and status (loading/empty states). Reuses @chenzy-design/core pure functions (offsetToIndex/indexToOffset/keyboardTarget/wrapIndex/cyclicCenterIndex/cyclicRecenter/momentumStep/projectSettleIndex/fixedRange).',
  OverflowList: 'An overflow list: collapse mode tucks overflow items into a +N collapse node (collapseFrom decides folding from the end or start), or scroll mode keeps items unfolded in a scrollable container; direction supports horizontal/vertical. Uses imperative ResizeObserver measurement + requestAnimationFrame batching + hysteresis to prevent jitter, reusing the @chenzy-design/core partition algorithm (computeOverflowPartition). Imperative methods (exposed via bind:this): forceMeasure() forces an immediate re-measure/re-compute (manually triggered when container size changes are not caught by RO); scrollTo(index, opts?) scrolls to a given item in scroll mode (opts.align start/center/end).',
  Spin: 'A loading indicator with inline / wrapper / fullscreen forms; delay debounce + minShowTime minimum display (reusing @chenzy-design/core createSpinController); role=status + aria-live=polite announces loading, with aria-busy on wrapped content; under reduced-motion the spin degrades to a breathing animation. spinning is controlled and not written back, with show/hide changes triggering only onShow/onHide.',
  Progress: 'A progress bar with line/circle/dashboard forms and normal/success/error/warning states; percent is clamped to 0..100, showInfo/format control the info area, success on the ring shows a checkmark (a non-color-redundant signal), and indeterminate shows a looping animation; role=progressbar, controlled percent not written back, notifying only via onComplete/onChange.',
  Skeleton: 'A skeleton screen: loading toggles between placeholder and real content, active enables a shimmer animation; provides five atomic shapes — Title / Paragraph / Avatar / Image / Button — for free composition; the placeholder container is aria-busy + aria-live=polite and skeleton blocks are aria-hidden; under reduced-motion the animation is disabled, degrading to a static base color. loading is controlled and not written back.',
  Banner: 'A banner notification with info/success/warning/danger semantics, full (filled) / card (boxed) forms, a title + description + semantic icon; closable controlled close, role/aria-live inferred from type and dynamic, a close transition, and reduced-motion fallback. Embedded in the document flow, non-modal, does not steal focus, and does not lock scrolling. Controlled open is not written back, notifying only via onOpenChange/onClose.',
  Modal: 'A modal dialog: a fixed mask + panel, portaled to body (or getContainer) to escape the parent stacking context, role=dialog aria-modal; useFocusTrap captures and restores focus, Esc/mask click close (controlled by keyboard/maskClosable), useScrollLock locks background scroll; head/body/foot structure, with ok/cancel default buttons replaceable by a custom footer and a danger okType; reduced-motion disables transitions. Controlled open is not written back, notifying only via onOpenChange/onCancel. destroyOnClose unmounts content on close and rebuilds on reopen (default false retains DOM). draggable lets you drag the panel by the title bar (offset-derived transform layered onto positioning, reset on reopen, pointer events bound to window). Stacking z-index is assigned by a module-level counter (shared by declarative and imperative use, later-opened on top, reclaimed on close). Imperative factories Modal.confirm/info/success/warning/error (also destructurable as modal): mount a temporary host on body, async onOk auto-loading, returning { destroy, update }.',
  Popconfirm: 'A popconfirm: in-place secondary confirmation anchored to the trigger element. Supports title/content, danger grading via type (default/warning/danger), confirm/cancel dual buttons, 4-way placement; triggerType=click/hover, loading on the confirm button when onConfirm returns a Promise (resolve closes / reject stays), getPopupContainer custom overlay container; destroyOnClose unmounts the overlay on close, arrowPointAtCenter points the arrow at the center, okButtonProps/cancelButtonProps pass through button props, motion entrance animation (reduced-motion fallback); role=dialog non-modal, reusing useDismiss/useFocusTrap/useFloating.',
  Drawer: 'A drawer overlay: a panel sliding in flush against one of four edges (left/right/top/bottom), reusing the Modal overlay infrastructure — portaled to body (or getContainer) to escape the parent stacking context, role=dialog aria-modal; useFocusTrap captures and restores focus, useDismiss Esc close (closeOnEsc), and with a mask useScrollLock locks background scroll; mask/maskClosable mask-click close, closable top-right close button; size (small/default/large) preset width/height, with width/height overriding; head/body/foot structure. The slide-in transition is CSS transform/opacity (token duration/easing, reduced-motion fallback); destroyOnClose unmounts content on close (rebuild on reopen), with onAfterClose after the exit transition ends. Stacking z-index shares the Modal modal/z-stack module-level counter, with later-opened on top in multi-layer stacking. With a title it uses aria-labelledby, otherwise aria-label. Aliased as SideSheet (identical behavior). Controlled open is not written back, notifying only via onOpenChange/onClose.',
  SideSheet: 'A side-sheet overlay: a container sliding in from any of the four viewport edges (left/right/top/bottom), carrying secondary task flows / details / filters / forms with a larger scrollable content area than Modal. Isomorphic with Modal, reusing the overlay infrastructure — portaled to body (or getContainer) to escape the parent stacking context; role=dialog, with aria-modal added when masked; useFocusTrap captures and restores focus (only when mask=true), useDismiss Esc close (closeOnEsc), and with a mask useScrollLock locks background scroll (disableable via disableScrollLock). mask=true is modal (mask + scroll lock + trap), mask=false is non-modal (no mask, no scroll lock, no focus stealing, closes on outside click when outsideClosable). size (small/default/large) presets width (horizontal) / height (vertical), with width/height explicitly overriding; near-full on narrow viewports. Header/Body/Footer three-part structure, with footer exposing close(). The slide-in transition is CSS transform/opacity (token duration/easing, reduced-motion and motionDisabled degrade to instant show/hide). lazyMount lazily mounts before first open, destroyOnClose unmounts content on close (with onAfterClose after the exit transition ends). Stacking z-index reuses the modal/z-stack module-level counter, unified with Modal’s layer stack, later-opened on top, Esc closing only the top of the stack. Controlled open (not written back), notifying only via onOpenChange({open, reason}), with reason covering esc/mask/outside/closeButton/programmatic.',
  Toast: 'A toast: an imperative global feedback API (Toast.info/success/warning/error/loading/open/promise/close/update/destroyAll). A singleton container lazily mounts to body; type-preset icons + auto-dismiss + hover pause + per-position maxCount FIFO eviction + same-id dedupe update; 6 positions stack independently (topLeft/top/topRight/bottomLeft/bottom/bottomRight, aligned with Notification) with multiple same-position toasts stacking vertically without overlap; theme (light/dark) card theme; Toast.promise(promise, { loading, success, error }) takes a Promise and auto-switches text on pending→resolve/reject (success/error may be functions receiving the value, with default text via @chenzy-design/locale); a11y announcements go through a singleton ARIA live region (error→assertive, others→polite), the card itself being purely visual with no role/aria-live.',
  Notification: 'A notification box: an imperative global feedback API (notification.open/success/info/warning/error/close/update/destroyAll). Richer than Toast — a title + content + icon; 6 positions stack independently; a singleton container lazily mounts to body; auto-close + hover pause + per-position maxCount FIFO eviction + same-id dedupe update; error/warning role=alert (assertive), others role=status (polite); an optional showProgress countdown progress bar (imperative rAF + hover-synced pause), a footer action-area Snippet, theme (light/dark); RTL (direction=rtl mirrors the card layout and swaps left/right positions), Esc closes the latest one (window keydown + cleanup), and notification.config({ direction, getPopupContainer }) configures the global direction and mount container (default ltr + body, aligned with Modal/Dropdown getContainer).',
  BackTop: 'A back-to-top floating button: shows/hides past a scroll threshold and scrolls smoothly back to top (easeInOutCubic), three sizes (small/default/large), a native button supporting Enter/Space, instant return under prefers-reduced-motion, and RTL-friendly (inset-inline-end). Listens to window scroll by default, with a custom scroll container specifiable via target; supports controlled visible and an announceOnArrive ARIA live announcement on reaching the top.',
  LocaleProvider: 'A language context injection component, renderless with no DOM output, injecting a LocaleApi (t/formatDate/formatNumber/direction) via setContext for descendant components to consume; includes a fallback chain + Intl cache + direction inference, and is a locale-dimension subset of ConfigProvider. locale accepts a language-pack object or a string code (registerLocale registry + built-in table lookup); when nested, inherit deep-merges the parent language pack (child overrides parent); timeZone/currency are injected into formatDate/formatNumber via context.',
  ConfigProvider: 'A global configuration entry that injects locale/theme/dir/size/zIndexBase/transition/reducedMotion and other config context via setContext for the subtree to inherit; when nested it shallow-merges (undefined inherits, nearest wins); theme="dark" writes data-theme on the wrap root to trigger the tokens dark palette, theme="auto" imperatively listens to prefers-color-scheme via matchMedia to resolve light/dark live (auto-responds to system changes, with cleanup); reducedMotion (boolean|"auto") controls animation fallback, writing a global flag when explicitly enabled (on the wrapping div when wrapping, imperatively on documentElement’s data-reduced-motion when renderless) to degrade animations depending on the motion-duration token, with "auto" following the system’s native @media; wrap=true renders a display:contents wrapper establishing an independent theme/direction scope, with the default wrap=false being renderless with no DOM; the locale dimension reuses LocaleProvider’s createLocale + LOCALE_CONTEXT_KEY mechanism; getPopupContainer is exposed via context as the global default overlay container for overlay components (Modal/Dropdown etc.) to fall back to when their own prop is unset; getValidateMessages is exposed via context as a global form-validation message override for Form to override over locale built-in text by Form.* keys (with interpolation support); when wrapping, as customizes the wrapper element tag (section/article/main etc., default div). Functional configs (getPopupContainer/getValidateMessages) do not participate in core pure config merging, being merged at the context layer by ConfigProvider with nearest-wins (inheriting the parent when not provided).',
  ResizeObserver: 'A size-observing utility component that wraps the native ResizeObserver (normalized via @chenzy-design/core), rendering a visually styleless wrapper element as the observed container, exposing width/height/entry via slot, and dispatching normalized size events via onResize; includes built-in throttle/debounce and multiple multi-target observation; silently degrades on SSR/old browsers, with fallbackToWindow falling back to approximate window.resize observation; onResizeStart/onResizeEnd expose the first-frame and quiet-end boundary events of continuous changes; also exports a resize action for lightweight use:resize usage and getGlobalResizeObserver as a singleton pool for large lists to share. Box models support content-box/border-box/device-pixel-content-box, and the wrapper element tag is customizable via tag.',
  LottieIcon: 'A Lottie animated icon: dependency-injected player factory (the library is not bound to lottie-web, users bring their own), data inline JSON or src URL async fetch (AbortController + loading skeleton / error state), size/color/trigger (auto/hover/manual)/loop/speed playback control, segments frame ranges ([start,end] or named markers), flipRtl RTL horizontal mirroring, visible controlled visibility (false pauses + display:none), canvas/renderer rendering backend (svg/canvas/html, passed through via adapter), imperative play/pause/stop, prefers-reduced-motion fallback to a static first frame, decorative aria-hidden / functional role=img + label.',
};
