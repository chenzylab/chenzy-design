<script lang="ts">
  // Icon 的 aria-label 默认为组件文件名（IconHome → 'home'），
  // 有更好的语义化名字时可通过 aria-label 传入（严格复刻 Semi Accessibility demo）。
  import { IconHome } from '@chenzy-design/icons';
</script>

<IconHome aria-label="back to homepage" />
