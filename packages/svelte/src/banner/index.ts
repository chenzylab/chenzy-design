export { default as Banner } from './Banner.svelte';
export { meta as bannerMeta } from './meta.js';
/** Banner 类型，对齐 Semi（info / success / danger / warning）。 */
export type BannerType = 'info' | 'success' | 'danger' | 'warning';
