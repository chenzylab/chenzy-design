/**
 * 对齐 Semi semi-foundation/datePicker/_utils/isValidTimeZone.ts。
 * 逻辑统一到 core（timePicker/datePicker 共用同一份），此处 re-export 保留 Semi 文件结构。
 */
import { isValidTimeZone } from '@chenzy-design/core';

export default isValidTimeZone;
